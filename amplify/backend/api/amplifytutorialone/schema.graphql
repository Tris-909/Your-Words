type Todo @model {
  id: ID!
  userId: ID!
  name: String!
  description: String
  image: String
  x: Float
  y: Float
}

type User @model {
  id: ID!
  username: String
  type: String
  boardHeight: Int
}
